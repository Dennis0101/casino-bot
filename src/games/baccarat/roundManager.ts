import { CFG } from '../../config.js';

// TODO: commit 공개 → BETTING 타이머 → 결과 산출 → 정산 → reveal → 다음 라운드
export async function ensureBacLoop(channelId: string) {
  // 라운드 스케줄러 골조 (추후 구현)
  return channelId; // placeholder
}
